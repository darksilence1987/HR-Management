apiVersion: v1
kind: Service
metadata:
  name: config-cluster
spec:
  type: ClusterIP
  selector:
    app: config-service
  ports:
    - port: 8889
      targetPort: 8889
      name: config-service-port
