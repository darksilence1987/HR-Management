server:
  port: 8889

spring:
  application:
    name: config-server-git
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/darksilence1987/HRManagement-Config.git"
          default-label: main
          skip-ssl-validation: true
